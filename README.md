# transaction-report-parser
Python ETL pipeline that parses multi-format raw transaction reports (fixed-width &amp; pipe-delimited), enriches them with terminal lookup data, and exports formatted Excel reports.
